
  <% include ../application/_header.ejs %>
  <% include ../application/_navbar.ejs %>

<div class="container content">

  <% if(adminUser) { %>
    <form action="/admin/stories<%= story.id %>/delete">
      <button class="button" type="submit">Delete Story</button>
    </form>

    <a class="button" href="/admin/stories/<%= story.id %>/edit">Edit Story</a>

    <% } %>
	<section class="section">
		<div class="article-title has-text-centered">
			<h1 class="title is-1"><%= story.title %></h1>
		</div>
	</section>
	<section class="section columns">

		<div class="column is-12 content-container">
			<%- story.content %>
		</div>
	</section>

</div>

<% include ../application/_footer.ejs %>
